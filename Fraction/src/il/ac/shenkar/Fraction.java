package il.ac.shenkar;

public class Fraction {
    private int numerator;
    private int  denominator;

    /**
     *  calculates a/b and returns reference to new object representing the answer
     * @param a - numerator
     * @param b - denominator
     */
    public static Fraction divide(Fraction a, Fraction b) throws FractionException {
        if() {
            throw new FractionException("Dived by 0 is not allowed");
        }

    }

}
